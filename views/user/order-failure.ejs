<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Order Failed</title>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f8f9fa;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        .failure-card {
            background-color: #fff;
            padding: 40px;
            border-radius: 15px;
            box-shadow: 0 0 20px rgba(0,0,0,0.1);
            text-align: center;
            max-width: 500px;
        }
        .failure-card img {
            width: 150px;
            margin-bottom: 20px;
        }
        .failure-card h2 {
            color: #dc3545;
            margin-bottom: 15px;
        }
        .failure-card p {
            color: #6c757d;
            margin-bottom: 30px;
        }
        .failure-card .btn {
            margin: 5px;
            padding: 10px 25px;
            border-radius: 30px;
        }
        .failure-card .btn:hover {
            transform: scale(1.05);
            transition: 0.2s;
        }
    </style>
</head>
<body>

    <div class="failure-card">
        <img src="https://cdn-icons-png.flaticon.com/512/564/564619.png" alt="Failed">
        <h2>Payment Failed</h2>
        <p>Unfortunately, we couldn’t complete your order due to a payment error.</p>
        <p><strong>Order ID(s):</strong> <%= orderIds %></p>

        <a href="/retry-payment?orderIds=<%= orderIds %>" class="btn btn-danger">Retry Payment</a>
        <a href="/orders" class="btn btn-outline-secondary">View Order Details</a>
    </div>

</body>
</html>
